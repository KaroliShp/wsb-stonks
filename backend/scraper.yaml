service: scraper
runtime: python37
entrypoint: gunicorn -b :$PORT scraper_main:app --timeout 720

includes:
  - env_variables.yaml

instance_class: B4_1G

basic_scaling:
  max_instances: 1
  idle-timeout: 30s